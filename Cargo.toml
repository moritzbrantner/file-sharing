[package]
name = "file-sharing"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"
description = "Local-first one-off file transfer and persistent folder sharing"
license = "MIT"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
